[package]
name = "zephyr"
version = "0.1.0"
edition = "2021"

[features]
default = []

inventory = ["dep:inventory"]

[dependencies]
inventory = { version = "0.3", optional = true }

[[example]]
name = "inventory"
required-features = ["inventory"]
